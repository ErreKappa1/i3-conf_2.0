Usage:

1) Install i3-gaps, polybar, rofi, xfce4-terminal [and all their dependencies asd] through your package manager (Example, using Arch's pacman: sudo pacman -S i3-gaps ...)

2) Remove the default config directories of polybar, i3-gaps and so on ( usually located in ~/.config/ )

3) Slap every directory here in your .config directory, done

Dat file is petty much useless, but y'know, dumbass niggas are everywere
